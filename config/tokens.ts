const tokenTypes = {
    ACCESS: 'access',
    REFRESH: 'refresh',
    RESET_PASSWORD: 'resetPassword',
    VERIFY_EMAIL: 'verifyEmail',
    SHOP_ACCESS: 'shopAccess'
};
  
const config = {
    tokenTypes
};

export default config;